name: Integration Tests

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  cypress:
    name: Cypress With Firebase Emulators
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run Cypress against Firebase emulators
        run: yarn test:e2e
