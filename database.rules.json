{
  "rules": {
    "committees": {
    	"$uid": {
        ".read": true,
        ".write": "auth != null && 
        	(!root.child('committees').child($uid).child('creatorUid').exists()
           || root.child('committees').child($uid).child('creatorUid').val() === auth.uid)"
      }
    }
  }
}
